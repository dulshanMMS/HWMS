"use strict";
/* eslint-disable no-param-reassign */
Object.defineProperty(exports, "__esModule", { value: true });
exports.transliterate = void 0;
const builtinReplacements_1 = require("./builtinReplacements");
const matchOperatorsRe = /[|\\{}()[\]^$+*?.]/g;
function escapeStringRegexp(str) {
    if (typeof str !== 'string') {
        throw new TypeError('Expected a string');
    }
    return str.replace(matchOperatorsRe, '\\$&');
}
const doCustomReplacements = (string, replacements) => {
    for (const [key, value] of replacements) {
        // TODO: Use `String#replaceAll()` when targeting Node.js 16.
        string = string.replace(new RegExp(escapeStringRegexp(key), 'g'), value);
    }
    return string;
};
const transliterate = (string, options) => {
    if (typeof string !== 'string') {
        throw new TypeError(`Expected a string, got \`${typeof string}\``);
    }
    options = {
        ...options,
        customReplacements: options.customReplacements || [],
    };
    const customReplacements = new Map([
        ...builtinReplacements_1.builtinReplacements,
        ...options.customReplacements,
    ]);
    string = string.normalize();
    string = doCustomReplacements(string, customReplacements);
    string = string
        .normalize('NFD')
        .replace(/\p{Diacritic}/gu, '')
        .normalize();
    return string;
};
exports.transliterate = transliterate;
//# sourceMappingURL=transliterate.js.map